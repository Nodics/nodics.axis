/**
 * Temporary Axis-owned presentation switches.
 *
 * These switches only control whether optional client affordances are rendered;
 * they never replace backend authorization or business capability contracts.
 */
export const axisPresentationFeatures = Object.freeze({
  favourites: false,
});
